package com.danieljoanol.pgsqlpopulator.model.enumarator;

public enum FieldType {
    CHAR, VARCHAR, TEXT, ENUM,
    SMALL_INT, INTEGER, BIG_INT, MONEY,
    BOOLEAN, UUID,
    DATE, TIME, TIMESTAMP
}
